# Default packages still come from the configured Python package index.
# Use direct wheel URLs for GPU PyTorch so pip does not jump back to the upstream PyTorch host.

opencv-python
numpy
tqdm
thop

# Windows + Python 3.12 + CUDA 12.8
torch @ https://mirrors.aliyun.com/pytorch-wheels/cu128/torch-2.11.0%2Bcu128-cp312-cp312-win_amd64.whl ; sys_platform == "win32" and python_version == "3.12"
torchvision @ https://mirrors.aliyun.com/pytorch-wheels/cu128/torchvision-0.26.0%2Bcu128-cp312-cp312-win_amd64.whl ; sys_platform == "win32" and python_version == "3.12"