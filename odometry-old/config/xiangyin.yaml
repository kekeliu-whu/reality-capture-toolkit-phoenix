common:
    lid_topic:  "/hesai/pandar"
    imu_topic:  "/alphasense/imu"
    time_sync_en: false         # ONLY turn on when external time synchronization is really not possible
    time_offset_lidar_to_imu: 0.0 # Time offset between lidar and IMU calibrated by other algorithms, e.g. LI-Init (can be found in README).
                                  # This param will take effect no matter what time_sync_en is. So if the time offset is not known exactly, please set as 0.0

preprocess:
    lidar_type: 4                # 1 for Livox serials LiDAR, 2 for Velodyne LiDAR, 3 for ouster LiDAR, 4 for hilti dataset
    scan_line: 32
    scan_rate: 10                # only need to be set for velodyne, unit: Hz,
    timestamp_unit: 0            # the unit of time/t field in the PointCloud2 rostopic: 0-second, 1-milisecond, 2-microsecond, 3-nanosecond.
    blind: 0.6

mapping:
    acc_cov: 0.1
    gyr_cov: 0.1
    b_acc_cov: 0.0001
    b_gyr_cov: 0.0001
    fov_degree:    180
    det_range:     100.0
    extrinsic_est_en:  true      # true: enable the online estimation of IMU-LiDAR extrinsic,
    # extrinsic_T: [ 0, 0, 0 ]
    # extrinsic_R: [ 0.999054, 0.0307174, -0.0307978,
    #               -0.0306697, 0.999528,  0.00201994,
    #                0.0308453, -0.00107347, 0.999524]
    extrinsic_T: [ -0.001180071, 0.032228949, 0.04972123 ]
    extrinsic_R: [ 0.999724558843918, 0.0210958636638412, -0.0102845019621837,
                 -0.0189728974426147, 0.984398208296437, 0.17492911897508,
                   0.013814326149529, -0.17468580949534, 0.984527314173656 ]
    # voxelmap settings
    ranging_cov: 0.02
    angle_cov: 0.05
    max_points_size: 600      # 200, 400, 700, 1000
    max_voxel_size: 0.8
    max_layer: 3
    layer_size: [5, 5, 5, 5, 5]
    min_eigen_value: 0.01         # 0.005, 0.01
    min_plane_likeness: 0.2

publish:
    path_en:  true
    scan_publish_en:  true       # false: close all the point cloud output
    dense_publish_en: true       # false: low down the points number in a global-frame point clouds scan.
    scan_bodyframe_pub_en: true  # true: output the point cloud scans in IMU-body-frame

pcd_save:
    pcd_save_en: true
    interval: 200                # how many LiDAR frames saved in each pcd file 
                                 # -1 : all frames will be saved in ONE pcd file, may lead to memory crash when having too much frames.

feature_extract_enable: 0
max_iteration: 5
filter_size_surf: 0.1
filter_size_map: 0.2
cube_side_length: 1000
runtime_pos_log_enable: true
